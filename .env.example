# =============================================================================
# IT Dor Services – Environment variables
# Copy this file to .env and fill in your values. Do not commit .env to git.
# =============================================================================

# -----------------------------------------------------------------------------
# Optional: Chat system debug logging (server + client). Set to 1 for verbose logs.
# On Vercel, chat uses in-memory store (no file writes); sessions/messages do not persist across serverless invocations. Set CHAT_USE_MEMORY=1 to force in-memory locally.
# -----------------------------------------------------------------------------
# CHAT_DEBUG=1
# NEXT_PUBLIC_CHAT_DEBUG=1
# CHAT_USE_MEMORY=1

# -----------------------------------------------------------------------------
# Contact form (Resend)
# -----------------------------------------------------------------------------
RESEND_API_KEY=
RESEND_FROM_EMAIL=onboarding@resend.dev
RESEND_TO_EMAIL=contact@itdorservices.com

# -----------------------------------------------------------------------------
# Live chat (Pusher) – optional; chat works without it (polling fallback)
# -----------------------------------------------------------------------------
NEXT_PUBLIC_PUSHER_KEY=
NEXT_PUBLIC_PUSHER_CLUSTER=us2
PUSHER_SECRET=
PUSHER_APP_ID=

# -----------------------------------------------------------------------------
# Admin chat – password for /admin/chat (cookie auth)
# -----------------------------------------------------------------------------
ADMIN_CHAT_PASSWORD=

# -----------------------------------------------------------------------------
# Booking – "Get Started" / consultation button (works with any email, e.g. GoDaddy)
# No Google account needed. Use a scheduler that works with your email:
#   • Calendly (calendly.com) – sign up with your GoDaddy/business email, create an event type, paste the link here
#   • Cal.com, or similar – same idea
# If unset, the button goes to /contact instead.
# -----------------------------------------------------------------------------
# NEXT_PUBLIC_BOOKING_URL=https://calendly.com/your-page/30min

# -----------------------------------------------------------------------------
# Telegram notifications – when a customer sends a message or starts a chat
# Get token from @BotFather; get chat IDs via getUpdates after user messages bot.
# On Vercel: set these in Project → Settings → Environment Variables (Production/Preview).
# -----------------------------------------------------------------------------
TELEGRAM_BOT_TOKEN=8644487623:AAGxgVzYtF1O9clksPEQU28__KWjOFgzxE4
TELEGRAM_ADMIN_IDS=6303496904
